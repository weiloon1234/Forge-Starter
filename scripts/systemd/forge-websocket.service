[Unit]
Description=Forge Starter — WebSocket Server
After=network.target postgresql.service redis.service
Wants=postgresql.service redis.service

[Service]
Type=simple
User=forge
Group=forge
WorkingDirectory=/opt/forge-starter
Environment=PROCESS=websocket
EnvironmentFile=/opt/forge-starter/.env
ExecStart=/opt/forge-starter/bin/forge-starter
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal
SyslogIdentifier=forge-websocket

[Install]
WantedBy=multi-user.target
