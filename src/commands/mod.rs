use forge::prelude::*;

pub fn register(_registry: &mut CommandRegistry) -> Result<()> {
    // Example:
    // registry.command(
    //     CommandId::new("import:users"),
    //     Command::new("import:users").about("Import users from CSV"),
    //     |inv| async move {
    //         let db = inv.app().database()?;
    //         // import logic...
    //         Ok(())
    //     },
    // )?;

    Ok(())
}
